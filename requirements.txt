# FinSight full development requirements. Python 3.11+.
# For production runtime only, use requirements-serve.txt.

# ----- Core -----
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.7.0

# ----- LLM + embeddings -----
anthropic==0.42.0
openai==1.59.5
voyageai==0.3.2
cohere==5.13.4

# ----- Orchestration -----
langchain==0.3.13
langchain-core==0.3.28
langchain-anthropic==0.3.1
langchain-community==0.3.13
langgraph==0.2.60
langsmith==0.2.7

# ----- Vector store -----
qdrant-client==1.12.1

# ----- Retrieval + reranking -----
rank-bm25==0.2.2
fastembed==0.8.0  # BM25 sparse vectors for native hybrid dense+sparse RRF in Qdrant
sentence-transformers==3.3.1  # for bge-m3 local fallback + ms-marco cross-encoder fallback

# ----- Data -----
duckdb==1.1.3
pandas==2.2.3
numpy>=1.26,<2.0  # langchain 0.3.x pins numpy<2 on Python 3.11
pyarrow==18.1.0

# ----- Serving -----
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1

# ----- UI -----
streamlit==1.41.1

# ----- Eval -----
ragas==0.2.9
datasets==3.2.0  # ragas dependency + BEIR future

# ----- Experiment tracking -----
mlflow==2.19.0

# ----- Guardrails -----
presidio-analyzer==2.2.356
presidio-anonymizer==2.2.356

# ----- Ingestion utilities -----
beautifulsoup4==4.12.3  # SEC filings HTML parsing
lxml==5.3.0
requests==2.32.3  # SEC EDGAR HTTP client
# yfinance removed — broken API as of 2024; using Stooq bulk download instead

# ----- Load test -----
locust==2.32.6

# ----- Testing + CI -----
pytest==8.3.4
pytest-asyncio==0.25.1
pytest-cov==6.0.0

# ----- Notebooks + viz -----
jupyter==1.1.1
matplotlib==3.10.0
seaborn==0.13.2
plotly==5.24.1

# ----- DVC -----
dvc==3.58.0

# ----- Dev tools -----
ruff==0.8.5

# FinSight runtime requirements for production serving (Render / Docker).
# Minimal by design: only what the Week 1 API + UI serve path imports, to keep
# the image small and cold starts fast on Render's free tier.
#
# NOT included (added when their feature lands): torch/sentence-transformers
# (bge-m3 fallback — deploy uses the Voyage API instead), langchain/langgraph
# (Week 2 pipeline), presidio (Week 4 guardrails). Keep this file lean.

python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.7.0

# LLM + embeddings (API clients — no local models)
anthropic==0.42.0
voyageai==0.3.2
cohere==5.13.4
certifi>=2024.2.2         # explicit CA bundle for TLS on slim base image

# Vector store + structured store
qdrant-client==1.12.1
duckdb==1.1.3             # retriever import; deploy reads text from Qdrant payload
fastembed==0.8.0          # BM25 sparse vectors for hybrid query (onnxruntime ~73MB, fits free tier)

# Serving
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1

# UI (separate Render service)
streamlit==1.41.1
